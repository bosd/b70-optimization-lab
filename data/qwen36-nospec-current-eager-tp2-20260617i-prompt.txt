Write a technical latency plan for an Intel Arc Pro B70 Qwen3.6 INT8 inference server.

The current service uses vLLM XPU, tensor parallelism, graph capture, Quark W8A8 INT8 weights, and exact quality canaries. Speculative decoding must preserve final verifier output. The current service uses vLLM XPU, tensor parallelism, graph capture, Quark W8A8 INT8 weights, and exact quality canaries. Speculative decoding must preserve final verifier output. The current service uses vLLM XPU, tensor parallelism, graph capture, Quark W8A8 INT8 weights, and exact quality canaries. Speculative decoding must preserve final verifier output. The current service uses vLLM XPU, tensor parallelism, graph capture, Quark W8A8 INT8 weights, and exact quality canaries. Speculative decoding must preserve final verifier output. The current service uses vLLM XPU, tensor parallelism, graph capture, Quark W8A8 INT8 weights, and exact quality canaries. Speculative decoding must preserve final verifier output. The current service uses vLLM XPU, tensor parallelism, graph capture, Quark W8A8 INT8 weights, and exact quality canaries. Speculative decoding must preserve final verifier output. The current service uses vLLM XPU, tensor parallelism, graph capture, Quark W8A8 INT8 weights, and exact quality canaries. Speculative decoding must preserve final verifier output. The current service uses vLLM XPU, tensor parallelism, graph capture, Quark W8A8 INT8 weights, and exact quality canaries. Speculative decoding must preserve final verifier output. The current service uses vLLM XPU, tensor parallelism, graph capture, Quark W8A8 INT8 weights, and exact quality canaries. Speculative decoding must preserve final verifier output. The current service uses vLLM XPU, tensor parallelism, graph capture, Quark W8A8 INT8 weights, and exact quality canaries. Speculative decoding must preserve final verifier output. The current service uses vLLM XPU, tensor parallelism, graph capture, Quark W8A8 INT8 weights, and exact quality canaries. Spec

Continue with dense numbered engineering notes. Focus on single-request decode speed, reliability gates, and no quality loss.
