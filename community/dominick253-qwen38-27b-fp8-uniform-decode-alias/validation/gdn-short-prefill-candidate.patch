--- a/vllm/v1/attention/backends/gdn_attn.py
+++ b/vllm/v1/attention/backends/gdn_attn.py
@@ -251,7 +251,14 @@
 
         if spec_sequence_masks is None:
             num_decodes, num_prefills, num_decode_tokens, num_prefill_tokens = (
-                split_decodes_and_prefills(m, decode_threshold=1)
+                split_decodes_and_prefills(
+                    m,
+                    decode_threshold=1,
+                    # Real target batches carry phase metadata. A fresh one-token
+                    # prefill must initialize state via the prefill kernels.
+                    # Preserve legacy behavior for metadata-less draft/capture callers.
+                    treat_short_extends_as_decodes=m.is_prefilling is None,
+                )
             )
             num_spec_decode_tokens = 0
             spec_token_indx = None
